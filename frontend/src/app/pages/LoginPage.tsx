export function LoginPage() {
  return (
    <main className="login-page">
      <section className="welcome-card">
        <p className="eyebrow">
          GCMS Admin
        </p>

        <h1>Iniciar sesión</h1>

        <p>
          La autenticación con FastAPI se
          implementará en SCRUM-112.
        </p>
      </section>
    </main>
  );
}